{
    "name": "auth",
    "$schema": "../../node_modules/nx/schemas/project-schema.json",
    "sourceRoot": "library/auth/src",
    "projectType": "library",
    "tags": ["auth", "shared"],
    "targets": {
        "test": {
            "executor": "nx:run-commands",
            "dependsOn": ["utils:build"],
            "options": {
                "command": "vitest run --config library/auth/vitest.config.ts",
                "cwd": "{workspaceRoot}"
            }
        },
        "build": {
            "executor": "@nx/esbuild:esbuild",
            "outputs": ["{options.outputPath}"],
            "options": {
                "outputPath": "library/auth/dist",
                "main": "library/auth/src/index.ts",
                "tsConfig": "library/auth/tsconfig.lib.json",
                "format": ["esm"],
                "declarationRootDir": "library/auth/src"
            }
        },
        "generate": {
            "executor": "nx:run-commands",
            "dependsOn": ["utils:build"],
            "options": {
                "command": "bun --env-file=.env x auth@latest generate --config library/auth/src/lib/auth.config.ts --yes",
                "cwd": "{workspaceRoot}"
            }
        }
    }
}
