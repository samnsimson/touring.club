// This file is auto-generated by scripts/generate-client-apis.ts — do not edit directly.
{{#each services}}
import { {{this.pascalService}}Sdk } from '@tc/api-sdk/clients/{{this.service}}';
{{/each}}
{{#each services}}
import { createClient as create{{this.pascalService}}Client } from '@tc/api-sdk/clients/{{this.service}}/client';
{{/each}}
import { ClientApiOptions } from './contract/client-api.contract';

export class ClientApi {
    constructor(private readonly options: ClientApiOptions) {}
{{#each services}}

    public get {{this.camelService}}() {
        return new {{this.pascalService}}Sdk({
            client: create{{this.pascalService}}Client({
                baseUrl: this.options.baseUrl,
                credentials: this.options.credentials ?? 'include',
                ...(this.options.auth !== undefined ? { auth: this.options.auth } : {}),
            }),
        });
    }
{{/each}}
}
