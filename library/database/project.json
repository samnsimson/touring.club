{
    "name": "database",
    "$schema": "../../node_modules/nx/schemas/project-schema.json",
    "sourceRoot": "library/database/src",
    "projectType": "library",
    "tags": ["database", "shared"],
    "targets": {
        "build": {
            "executor": "@nx/esbuild:esbuild",
            "outputs": ["{options.outputPath}"],
            "options": {
                "outputPath": "library/database/dist",
                "main": "library/database/src/index.ts",
                "tsConfig": "library/database/tsconfig.lib.json",
                "declarationRootDir": "library/database/src"
            }
        },
        "migration:run": {
            "executor": "nx:run-commands",
            "dependsOn": ["config:build", "utils:build"],
            "options": {
                "command": "TS_NODE_PROJECT=library/database/tsconfig.orm.json bun --bun library/database/src/run-migrations.ts",
                "cwd": "{workspaceRoot}"
            }
        },
        "migration:generate": {
            "executor": "nx:run-commands",
            "dependsOn": ["config:build", "utils:build"],
            "options": {
                "command": "TS_NODE_PROJECT=library/database/tsconfig.orm.json bunx typeorm-ts-node-esm migration:generate library/database/src/migrations/{args.name} --dataSource=library/database/src/database.datasource.ts",
                "cwd": "{workspaceRoot}"
            }
        },
        "test": {
            "executor": "@nx/jest:jest",
            "options": {
                "jestConfig": "library/database/jest.config.cts",
                "passWithNoTests": true
            }
        }
    }
}
