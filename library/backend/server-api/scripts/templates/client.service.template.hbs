// This file is auto-generated by scripts/generate-server-apis.ts — do not edit directly.
import { Injectable } from '@nestjs/common';
import { ConfigService } from '@nestjs/config';

{{#each services}}
import { {{this.pascalService}}Sdk } from '@tc/api-sdk/clients/{{this.service}}';
{{/each}}

{{#each services}}
import { createClient as create{{this.pascalService}}Client } from '@tc/api-sdk/clients/{{this.service}}/client';
{{/each}}

@Injectable()
export class ServerApi {
    constructor(private readonly configService: ConfigService) {}
{{#each services}}

    public get {{this.camelService}}() {
        const baseUrl = this.configService.get('{{this.envVar}}');
        const client = create{{this.pascalService}}Client({ baseUrl, throwOnError: true });
        return new {{this.pascalService}}Sdk({ client });
    }
{{/each}}
}
