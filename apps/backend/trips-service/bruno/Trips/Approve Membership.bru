meta {
  name: Approve Membership
  type: http
  seq: 13
}

post {
  url: {{baseUrl}}/api/v1/trips/{{tripId}}/members/{{membershipId}}/approve
  body: none
  auth: bearer
}

headers {
  Authorization: Bearer {{accessToken}}
}

docs {
  Approve a pending membership request.
}
