meta {
  name: Archive Trip
  type: http
  seq: 7
}

post {
  url: {{baseUrl}}/api/v1/trips/{{tripId}}/archive
  body: none
  auth: bearer
}

headers {
  Authorization: Bearer {{accessToken}}
}

docs {
  Archive a cancelled or published trip.
}
