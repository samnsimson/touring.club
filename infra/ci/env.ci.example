# Copy values into GitHub Actions repository/environment secrets, or export before running pipelines locally.
NODE_ENV=test
CI=true
NX_NO_CLOUD=true

HOST=0.0.0.0
AUTH_SERVICE_PORT=3000
PORT=3000

DATABASE_URL=postgresql://postgres:postgres@postgres:5432/touring_club
BETTER_AUTH_SECRET=local-dev-better-auth-secret-32chars
AUTH_APP_URL=http://localhost:3000
EMAIL_PROVIDER=console
